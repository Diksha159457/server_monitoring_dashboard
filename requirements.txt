# requirements.txt — Python package dependencies for the Server Monitoring Dashboard
# Install all dependencies with: pip install -r requirements.txt

flask>=3.0.0   # Flask: the core Python web framework (routes, templates, static files, dev server)
gunicorn>=21.2.0
# Gunicorn: production WSGI server for running the Flask app on Render.com (not needed for local development)